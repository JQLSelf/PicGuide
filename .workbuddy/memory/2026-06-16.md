# 2026-06-16 工作日志

## 文件夹树图片数量明暗适配
- page_browser.dart: 4处 Colors.grey 改为 Theme.of(context).brightness 判断（暗色 white70/white54，亮色 black54/black45）

## 扫描算法重构：Isolate 真并行
- service_media_scanner.dart: 全面重写，删除 sequential/parallel/adaptive 三算法，改为 Isolate 真并行
  - compute(_computeFileMeta) 在独立 Isolate 中并行计算 MD5 + EXIF
  - 并发数 = Platform.numberOfProcessors.clamp(2, 8)
  - DB 操作 + GPS 反查保留在主 isolate
  - 新增 _indexFileWithMeta 方法接受预计算元数据
  - 保留 _indexFile 方法给单文件导入使用
- 删除 model_scan_algorithm.dart（三算法配置模型）
- 删除 service_device_info.dart（设备信息检测，仅被算法选择弹窗使用）
- 删除 SCAN_ALGORITHM_IMPLEMENTATION.md（算法实现文档）
- dialog_scan_config.dart: 简化为确认弹窗（移除设备信息卡片和算法选择）
- page_browser.dart: 移除算法参数传递，_scanFolder 不再接受 algorithm 参数
- README.md: 更新扫描描述为"Isolate 多核并行"，移除三算法相关内容
- assets/USER_MANUAL.md: 移除 2.2 扫描算法选择章节，简化为确认弹窗说明

## Step 4 时间轴索引表改造（2026-06-16 下午）
### 数据库层
- database.dart: 添加 `MediaDateIndexes` 表定义（dateKey, count, firstOffset, updatedAt）
- database.dart: 升级 schemaVersion 到 4，添加 v3->v4 迁移（创建新表）
- database.dart: 添加时间轴索引表操作方法：
  - `upsertDateIndex()` - 插入或更新日期索引
  - `getDateIndexes()` - 获取所有日期索引（按 dateKey 降序）
  - `deleteDateIndex()` - 删除指定日期的索引
  - `rebuildDateIndexes()` - 全量重建日期索引（用于全盘重扫）
  - `decrementDateIndexCount()` - 减少指定日期的计数（用于软删除）
  - `getDateOffset()` - 获取某日期的起始偏移量（实时计算）
  - `_getItemDateTaken()` - 获取照片拍摄日期（优先 EXIF，回退 fileModifiedAt）
  - `_formatDateKey()` - 格式化日期为 YYYY-MM-DD

### 扫描器层
- service_media_scanner.dart: 添加 `ScanPhase.rebuildingIndex` 枚举值
- service_media_scanner.dart: 在 `scanFolder()` 方法中，对账阶段之后添加重建时间轴索引逻辑
- 扫描完成后自动重建时间轴索引表，确保索引准确性

### 软删除维护
- database.dart: 修改 `softDeleteMedia()` 方法，在软删除之前先减少时间轴索引表中对应日期的计数

### UI 层
- widget_scan_progress.dart: 修复 `_phaseLabel()` 方法的 switch 语句，添加对 `ScanPhase.rebuildingIndex` 的处理（显示"更新时间轴索引中"）

### 代码生成
- 运行 `flutter packages pub run build_runner build --delete-conflicting-outputs` 成功生成 `database.g.dart`
- 运行 `dart analyze` 检查，无错误

### 待完成
- 修改时间轴侧边栏代码，使用 `MediaDateIndexes` 表加速（当前仍在内存中计算日期分组）
- 优化 `rebuildDateIndexes()` 方法性能（当前逐个查询 EXIF 表，存在 N+1 查询问题）
